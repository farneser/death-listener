name: DeathListener
version: '${project.version}'
main: dev.farneser.deathslistener.DeathListener
api-version: '1.20'
author: farneser
commands:
  dlist:
    description: Show latest deaths
    usage: /dlist page
    permission: deaths-list.dlist
permissions:
  deaths-list.dlist:
    default: op
    description: Allow command to show latest deaths for player
  deaths-list.dlist.admin:
    default: op
    description: Update command to show latest deaths for all players
    children:
      - deaths-list.dlist
